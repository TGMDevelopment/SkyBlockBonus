package ga.matthewtgm.skyblockmod.listeners;

import ga.matthewtgm.lib.util.LoggingUtils;
import ga.matthewtgm.skyblockmod.Constants;
import org.apache.logging.log4j.Logger;

public class SkyBlockModListener {
    protected Logger logger = LoggingUtils.getInstance().createClassLogger(Constants.NAME, this.getClass());
}